package org.omich.lang;import org.omich.lang.SQLite.WordsStorage;import org.omich.lang.httpClient.SmdClient;import android.app.Service;import android.content.Intent;import android.os.IBinder;import android.util.Log;public class SyncService extends Service {            LangOmichSettings lSettings;           @Override    public IBinder onBind(Intent intent) {        return null;    }    @Override    public void onCreate() {         lSettings = new LangOmichSettings(this,LangOmichActivity.SETTINGS_NAME);    }    @Override    public void onDestroy() {    }    @Override    public void onStart(Intent intent, int startid) {                 SmdClient client = new SmdClient();                String login = lSettings.getLogin();        String password = lSettings.getPassword();                try{        	        	if(client.auth(login, password)){        		        		WordsStorage wordsStorage = new WordsStorage(this);        		//TODO получить удаленные слова и отправить их для удаления на сервер        		//TODO получить слова новые и модифицировнный слова для отправки на сервер        		Log.d("test", "start");        		wordsStorage.open();        		//TODO использовать addWords вместо getWords        		wordsStorage.synchronization(client.getWords());        		wordsStorage.close();        		Log.d("test", "end");        	}        	        }catch (Exception e) {			// TODO: handle exception		}    }}